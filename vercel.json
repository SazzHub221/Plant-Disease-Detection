{
  "buildCommand": "export NODE_OPTIONS=--openssl-legacy-provider && npm run build && npm install -g serve && serve -s build",
  "outputDirectory": "build",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/main.py"
    }
  ]
} 